<!DOCTYPE html>
<html>
	<head>
		<title>Servidor Activacion ehmSoftware</title>
		<%= stylesheet_link_tag    "application", :media => "all" %>
		<%= javascript_include_tag "application" %>
		<%= csrf_meta_tags %>
	</head>
	<body>
		<% if signed_in? %>
			<%= link_to "Sign out", signout_path, method: "delete" %>

		<% end %>

		<%= yield %>

	</body>
</html>
